openapi: 3.2.0
info:
  title: Polar Subscriptions API
  description: Polar is an open-source Merchant of Record (MoR) and monetization platform for developers.
  termsOfService: https://polar.sh/legal/terms
  contact:
    name: Polar Support
    email: support@polar.sh
    url: https://polar.sh/docs
  license:
    name: Apache-2.0
    url: https://github.com/polarsource/polar/blob/main/LICENSE
  version: 2026-04
servers:
- url: https://api.polar.sh/v1
  description: Production environment
- url: https://sandbox-api.polar.sh/v1
  description: Sandbox environment
security:
- oat: []
tags:
- name: Subscriptions
  description: Recurring subscriptions.
paths:
  /subscriptions/:
    get:
      operationId: subscriptions:list
      tags:
      - Subscriptions
      summary: List Subscriptions
      responses:
        '200':
          description: A paginated list of subscriptions.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListResourceSubscription'
  /subscriptions/{id}:
    get:
      operationId: subscriptions:get
      tags:
      - Subscriptions
      summary: Get Subscription
      parameters:
      - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: The requested subscription.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subscription'
    patch:
      operationId: subscriptions:update
      tags:
      - Subscriptions
      summary: Update Subscription
      parameters:
      - $ref: '#/components/parameters/IdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: The updated subscription.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subscription'
    delete:
      operationId: subscriptions:revoke
      tags:
      - Subscriptions
      summary: Revoke Subscription
      parameters:
      - $ref: '#/components/parameters/IdPath'
      responses:
        '200':
          description: The revoked subscription.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subscription'
components:
  schemas:
    ListResourceSubscription:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Subscription'
        pagination:
          $ref: '#/components/schemas/Pagination'
    Subscription:
      type: object
      properties:
        id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        status:
          type: string
          enum:
          - incomplete
          - trialing
          - active
          - past_due
          - canceled
          - unpaid
        current_period_start:
          type: string
          format: date-time
        current_period_end:
          type:
          - string
          - 'null'
          format: date-time
        cancel_at_period_end:
          type: boolean
        product_id:
          type: string
          format: uuid
        customer_id:
          type: string
          format: uuid
        recurring_interval:
          type: string
          enum:
          - month
          - year
        amount:
          type:
          - integer
          - 'null'
        currency:
          type:
          - string
          - 'null'
    Pagination:
      type: object
      properties:
        total_count:
          type: integer
        max_page:
          type: integer
  parameters:
    IdPath:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  securitySchemes:
    oat:
      type: http
      scheme: bearer
      description: Organization Access Token used for backend, organization-scoped operations.
    pat:
      type: http
      scheme: bearer
      description: Personal Access Token scoped to a user.
    customer_session:
      type: http
      scheme: bearer
      description: Customer Session token used for customer portal endpoints.
